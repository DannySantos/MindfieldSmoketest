<div class="consumer container-fluid">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <%= form_tag(consumers_path, method: :post) do %>
        <%= render @questions %>
        <%= submit_tag "Submit", class: "form-control btn btn-wide" %>
      <% end %>
    </div>
  </div>
</div>
